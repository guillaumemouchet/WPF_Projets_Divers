<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:AnalogClockControl.CustomControls">

    <Style TargetType="local:DigitalClock">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DigitalClock">
                    <ControlTemplate.Resources>
                        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                    </ControlTemplate.Resources>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TimeClock.Hour, StringFormat=D2, RelativeSource={RelativeSource TemplatedParent}}"></TextBlock>
                        <TextBlock x:Name="Colon" Text=":"></TextBlock>
                        <TextBlock Text="{Binding TimeClock.Minute, StringFormat=D2, RelativeSource={RelativeSource TemplatedParent}}"></TextBlock>
                        <TextBlock Text=":" Visibility="{TemplateBinding ShowSeconds, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding TimeClock.Second, StringFormat=D2, RelativeSource={RelativeSource TemplatedParent}}" Visibility="{TemplateBinding ShowSeconds, Converter={StaticResource BooleanToVisibilityConverter}}"></TextBlock>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>